import 'package:get/get.dart';

class InviteFamilyController extends GetxController {
  List name = List.generate(40, (index) => 'User Number $index');
  List numbers = List.generate(40, (index) => '030612345$index');
}
